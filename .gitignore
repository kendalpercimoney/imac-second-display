# SwiftPM build products
.build/

# Built apps are attached to releases rather than committed. Keeping them in
# the tree meant they went stale against the source between rebuilds, and a
# binary that does not match the code beside it is worse than no binary.
build/

# macOS
.DS_Store

# Xcode
*.xcuserdatad
xcuserdata/
