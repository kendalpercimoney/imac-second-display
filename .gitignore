# SwiftPM build products
.build/

# Ship the two app bundles, but nothing else that lands in build/
build/*
!build/LanScreenHost.app/
!build/LanScreenClient.app/

# macOS
.DS_Store

# Xcode
*.xcuserdatad
xcuserdata/
